# This is the environment file for the loader application.
PORT=8080

# Database configuration
DB_HOST=fedora-server.local
DB_PORT=5432
DB_USER=rdws_user
DB_PASSWORD=rdws_password
DB_NAME=rdws_us_dev

# services configuration
SERVICES_BASE_PATH=./services

LOG_LEVEL=info
RDWS_ENVIRONMENT=development